<?php

/**
 * Production overrides for www.bcefc.ca.
 * Kirby automatically loads this file instead of/on top of config.php
 * when the request host matches the filename (config.<hostname>.php).
 */
return [
    'debug' => false,
];
